@model GenElect.Models.IndexViewModel
@{
    ViewBag.Title = "Current Schedule";
}

<h2>@ViewBag.Title.</h2>

@*<p class="text-success">@ViewBag.StatusMessage</p>*@
<div>
    <h4>This is your account schedule</h4>
    <hr />
    <dl class="dl-horizontal">
        @if (ViewBag.User.Elective1 == null)
        {
            <dd>Elective 1: Not Chosen</dd>
        }
        else
        { 
            <dd>Elective 1: @ViewBag.User.Elective1 | @Html.ActionLink("Unregister","Unregister", new { ename = ViewBag.User.Elective1, period = 1 })</dd>
        }
        
        @if (ViewBag.User.Elective2 == null)
        {
            <dd>Elective 2: Not Chosen</dd>
        }
        else
        {
            <dd>Elective 2: @ViewBag.User.Elective2 | @Html.ActionLink("Unregister", "Unregister", new { ename = ViewBag.User.Elective2, period = 2 })</dd>
        }

        @if (ViewBag.User.Elective3 == null)
        {
            <dd>Elective 3: Not Chosen</dd>
        }
        else
        {
            <dd>Elective 3: @ViewBag.User.Elective3 | @Html.ActionLink("Unregister", "Unregister", new { ename = ViewBag.User.Elective3, period = 3 })</dd>
        }
    </dl>
</div>
